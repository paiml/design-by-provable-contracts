import ProvableContracts.Theorems.Money
import ProvableContracts.Theorems.Connection
import ProvableContracts.Theorems.SafeDiv

/-!
# ProvableContracts — Lean 4 root
Single import that pulls in all per-module theorem stubs.
Regenerate the per-theorem stub bodies with `pv lean --output-dir lean`.
-/
